import * as React from 'react';
import styled from 'styled-components';

interface IPagePixProps {}

const Container = styled.div``;

export const PagePix: React.FC<IPagePixProps> = () => {
    return <Container>pix</Container>;
};
